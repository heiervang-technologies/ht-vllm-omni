# Adversarial review — PR #21 duplicate diffusion FP8

Target: https://github.com/heiervang-technologies/ht-vllm-omni/pull/21  
Reviewed head: `9d8ccedf91fe7e047039ed9c764a62a4a050ab93`  
Live base at review: `main` at `11f59f600d64fb34fd45fe481a242e0ef9aa5e6e`

## CLAIMS

- **“Complements PR #9 with the full feature implementation” — REFUTED.** PRs #21 and #9 have byte-identical head SHAs. There is no complementary delta.
- **“Current merge candidate” — REFUTED.** This is the later draft (created 2026-02-28), targets `main` rather than the fork's default `ht`, has an empty template description, is 77 base commits behind, and has 16 synthetic-merge conflicts. PR #9 was created earlier and at least targets `ht`, but it too is obsolete.
- **“FP8 implementation is validated and fleet-safe” — UNPROVEN / REFUTED for Pascal.** No checks or results are attached. Runtime config has no device-capability gate; only the smoke test skips GPUs below 8.9.

The code-level findings are identical to [PR #9's review](PR-9.md).

## LOST-BODIES

- Duplicate-body count: exact same 13 commits and head tree as PR #9.
- Against live `ht`, the old head lacks the current generalized quantization, component routing, INT8, GGUF, INC/AutoRound, and associated tests/docs.
- Against its declared `main` base, the PR remains conflicting and mixes unrelated TTS/branding commits with FP8 work.

## PASCAL

**incompatible-because-fp8-does-not-exist-on-sm_61-and-no-runtime-gate-prevents-selection.**

## NEW-FAILURE-MODES

- Maintainers can review or merge two PR identities for one exact tree.
- Targeting `main` bypasses the intended `ht` integration line.
- Late FP8 failure remains possible on Pascal.

## VERDICT

**do-not-merge.** Close immediately as an exact duplicate; PR #9 should also close as obsolete.

**UPSTREAM: needs-split.** This duplicate has no independent upstream payload. Only a verified missing generic FP8 behavior, extracted once from the stale mixed branch and rebased on current upstream, could be a candidate; Pascal deployment policy stays in HT.
